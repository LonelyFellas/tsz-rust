# 词条标注需求

已获产品及实施批准。一个词条可含多个原型；独立词条共享原型时，每个直接同名组内须填写非空、不同的标注。无重复时不强制。创建第二/第三条时已有标注自动带出且可编辑，同一词条仅出现一次。原生弹窗两列“词条 / 标注”，保留词名及简短释义。点击“保存标注并创建”一次完成原子旧标注更新及新条创建，随后进入词形与发音。取消和任何错误不得部分修改。

列表在词名下返回并展示标注；以最小范围提供后续编辑，不扩大学习端。原型碰撞由后端权威计算，不把普通词形匹配当作同名。第三/更多条、历史无标注、编辑碰撞、并发、幂等、事务回滚为验收重点。接口与存储约束见 design.md。
